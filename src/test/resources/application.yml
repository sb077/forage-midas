spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: midas-core-test-group
      auto-offset-reset: earliest

logging:
  level:
    root: INFO
    com.jpmc.midascore: INFO